mutation CreateUser($input: UserSignupInput!) {
    createUser(input: $input) {
        id
        name
        bets {
            id
            homeTeamGoals
            awayTeamGoals
        }
    }
}
