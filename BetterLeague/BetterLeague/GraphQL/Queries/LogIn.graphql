query LogIn($input: UserLoginInput!) {
    logIn(input: $input) {
        id
        name
        points
        bets {
            id
            homeTeamGoals
            awayTeamGoals
        }
    }
}
